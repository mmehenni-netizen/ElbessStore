import 'package:elbess_store/features/Add/Widgets/addbody.dart';
import 'package:flutter/material.dart';

class Addview extends StatelessWidget {
  const Addview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Addbody(),
    );
  }
}